# Mac housekeeping files
.DS_Store

# Third-party reference material — kept locally, not published
uksrg.txt

# Working photo originals — kept locally, never published.
# Only optimised copies that the site actually uses go in assets/.
Photos/
